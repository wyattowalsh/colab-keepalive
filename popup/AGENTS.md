# Popup Agent Instructions

- Keep the popup vanilla HTML, CSS, and JavaScript. Do not add frameworks or build steps.
- Do not use inline scripts, inline event handlers, remote assets, or dynamic code execution.
- Keep controls keyboard-accessible with explicit labels, ARIA where useful, and dark-mode-friendly CSS.
- Popup changes must preserve the shared message envelope from the root `AGENTS.md`.

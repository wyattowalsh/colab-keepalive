# Tests Agent Instructions

- Keep tests dependency-free and runnable with Node's built-in `node:test`.
- Load shared browser helpers in an isolated VM context instead of requiring build tooling.
- Cover privacy, manifest scope, icon dimensions, settings validation, message validation, and Connect/Reconnect classification when those contracts change.
